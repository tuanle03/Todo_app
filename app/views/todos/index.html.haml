%p#notice= notice
%h1 Todos
%table
  %thead
    %tr
      %th Title
      %th Notes
      %th Status
      %th{:colspan => "3"}
  %tbody
    - @todos.each do |todo|
      %tr
        %td= todo.title
        %td= todo.notes
        - if todo.status
          %td= link_to 'Done', toggle_path(todo), method: :put
        - else
          %td= link_to 'Doing', toggle_path(todo), method: :put

        %td= link_to 'Show', todo
        %td= link_to 'Edit', edit_todo_path(todo)
        %td= link_to 'Destroy', todo, method: :delete, data: { confirm: 'Are you sure?' }
%br/
= link_to 'New Todo', new_todo_path
